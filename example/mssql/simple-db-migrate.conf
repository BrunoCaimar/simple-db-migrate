# example config file
import os

HOST = os.getenv("DB_HOST") or "canada-pc\sqlexpress"
USERNAME = os.getenv("DB_USERNAME") or "root"
PASSWORD = os.getenv("DB_PASSWORD") or "root123456"
DATABASE = os.getenv("DB_DATABASE") or "migration_example"

# relative path from the location of this file
MIGRATIONS_DIR = os.getenv("MIGRATIONS_DIR") or "."